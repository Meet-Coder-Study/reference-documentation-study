name: automerge
on:
  schedule:
    - cron: '* * * * *'
jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: automerge
        uses: "pascalgn/automerge-action@v0.13.1"
        env:
          MERGE_LABELS: ""

